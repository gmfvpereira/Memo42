%h1 Fact
%p
  %b Description
  %br
    = @fact.description
.form-actions
  = link_to 'Back', facts_path, :class => 'btn'
  = link_to 'Edit', edit_fact_path(@fact), :class => 'btn'
  = link_to 'Delete', fact_path(@fact), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger'
